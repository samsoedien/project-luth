// import { getServerAuthSession } from '~/server/auth'

export default async function SigninPage() {
  // const session = await getServerAuthSession()
  // if (!session?.user) return null

  return <div>Signin Content</div>
}
